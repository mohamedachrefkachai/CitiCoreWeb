<div class="container mt-4 p-4 rounded shadow-sm bg-light">
   

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

    <div class="mb-3">
        {{ form_label(form.contenu, null, {'label_attr': {'class': 'form-label fw-bold'}}) }}
        {{ form_widget(form.contenu, {'attr': {'class': 'form-control', 'rows': '4', 'placeholder': 'Saisissez le contenu de la réponse...'}}) }}
        {{ form_errors(form.contenu) }}
    </div>

    <div class="row">
        <div class="col-md-6 mb-3">
            {{ form_label(form.DateReponse, null, {'label_attr': {'class': 'form-label fw-bold'}}) }}
            {{ form_widget(form.DateReponse, {'attr': {'class': 'form-control'}}) }}
            {{ form_errors(form.DateReponse) }}
        </div>

        <div class="col-md-6 mb-3">
            {{ form_label(form.Statut, null, {'label_attr': {'class': 'form-label fw-bold'}}) }}
            {{ form_widget(form.Statut, {'attr': {'class': 'form-select'}}) }}
            {{ form_errors(form.Statut) }}
        </div>
    </div>

    <div class="text-center mt-4">
        <button type="submit" class="btn btn-success px-4 py-2 shadow-sm">
            💾 Enregistrer
        </button>
    </div>

    {{ form_end(form) }}
</div>
